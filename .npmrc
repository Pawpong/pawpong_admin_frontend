# pnpm — React/Vite/AntD 호환 위해 hoisted node_modules 사용 (backend 와 동일 정책).
# 일부 라이브러리(react 19 + antd v5 호환 패치 등)가 hoisting 가정해 깨질 수 있어 strict 분리는 피함.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
